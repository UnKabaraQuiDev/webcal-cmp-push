services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webcal-cmp-push
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${CONTAINER_PORT}:8080"
    environment:
    restart: unless-stopped

volumes:
  postgres_data: